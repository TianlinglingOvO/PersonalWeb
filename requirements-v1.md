# 个人网站需求规格（v1）

## 一句话目标
挂在社交账号简介里的 About me 站点：自我介绍 + 文章 + 项目/经历 + 可选服务占位，一眼能看懂「你是谁」。

## 交付策略（已定）
- 第一期先交付可预览的完整壳子：风格、排版、导航、各区块都做好
- 文案与素材全部用占位（假名字、假介绍、假项目、示例文章、服务占位卡片、占位图）
- 主人认可风格后再替换为真实信息；替换应只改 content/ 与静态资源，不改布局代码

## 静态 vs 动态（已定）
### 第一期：静态（Cloudflare Pages）——已定，对当前目标更合适
原因简述：
- 无评论、无登录、无支付/下单后端，不需要服务端会话
- 内容用 Markdown 即可维护
- 部署简单（连 Git，推送即部署）
- 与自有 VPS 隔离，站点不绑在同一台机器上

### 第二期（可选演进）
若以后需要在线编辑内容、表单后端、或其它服务端能力，再升级为动态，或「静态前端 + 轻量后端/无头 CMS」。
第一期代码应尽量让内容与布局分离（`content/`），方便以后迁移；**第一期不要实现动态后台**。

## 部署（已定）
- Cloudflare Pages 静态托管（连 Git 仓库，推送即部署）
- 不上自有 VPS；无服务器端数据库
- 境外托管，无需备案

## 访客与场景
- 从社交简介点进链接的陌生人
- 首页 3 秒内看清：名字、一句话定位、页面结构

## 视觉风格（二次元）
- 二次元/插画感，但信息清晰，文字不被背景盖住
- 浅色底 + 1～2 强调色；正文深灰高对比
- 可有背景插图；移动端减弱重背景
- 中文系统无衬线；轻微悬停/淡入动效

## 信息架构
### 首页（单页滚动 + 锚点）
1. Hero：头像占位 + 名字占位 + 一句话 + 快捷跳转
2. 关于我：Markdown 占位文案
3. 项目/经历/特长：至少 3 张占位卡片
4. 文章：至少 2 张卡片 → 独立文章页（示例 Markdown）
5. **服务（业务 / 服务）**：2～3 张占位卡片（标题、短简介、CTA）→ CTA 指向 Connect / 联系；可选卡片内锚点展开细节。优先首页区块以保持静态简单；若实现上更干净，也可做成简单 `/services` 页并从导航链接过去（二选一即可）
6. Connect with me：按下方「联系方式」分组展示（主联系优先；兴趣/游戏可折叠或图标区）
7. 页脚

### 文章页
标题、日期、Markdown 正文、返回首页

### 服务页（可选）
若选用独立 `/services` 而非首页区块：列出与首页相同的 2～3 张占位卡片，CTA 仍指向 Connect / 联系；第一期仍为静态页面，无支付、无下单表单后端。

## 业务 / 服务（已定 · 第一期仅占位）

### 目的
为日后可能提供的合法服务预留版面（例如技术协助、站点搭建、咨询等）。第一期只做**通用占位卡片**，主人以后再换成真实、合法的服务说明。

### 页面与 IA
- **推荐**：首页增加「服务」区块，2～3 张占位卡片（标题 + 短简介 + CTA，如「了解更多 / 联系我」）；可选详情用同页锚点展开，保持静态简单
- **亦可**：独立静态 `/services` 页并从导航链接（若比首页塞满更干净）
- 服务文案放在 `content/`（或同等数据文件），便于以后只改内容不改布局

### CTA
- CTA 指向 **Connect / 联系我**（锚点或联系区块），**不要**接结账、支付网关、购物车或订单系统

### 第一期范围
- 仅占位展示
- **不做**：支付、订单表单后端、库存、价格结算、自动履约

### 内容禁区与占位命名（明确）
- **不要**写入或暗示：VPN、机场、翻墙、代理节点分享，或同类违规/灰色业务文案
- 服务标题用中性占位，例如：「服务项目占位 A」「服务项目占位 B」「服务项目占位 C」
- 简介用中性占位句（如「这是一项待补充的服务说明」），不描述具体敏感业务

### 未来
主人自行把占位换成真实、合法的服务内容；届时仍建议 CTA 先走联系沟通，再视需要另做动态能力（属第二期可选演进）。

## Connect with me 联系方式（已定精简版）

### 主联系（常看、优先展示）
- **国内**：QQ、微信
  - 微信可用二维码图，或「点击复制」占位
  - 注意：若不想在站上明文泄露真实微信号，第一期先用占位，勿写真实号
- **国外**：X（Twitter）、Telegram、Gmail、**GitHub**（技术向联系；保留在主联系）

### 兴趣/游戏（次要一组，可折叠或图标区）
- 小黑盒、Steam
- **抖音**（可选）：每天看但可不放进主联系；若要放，放在「其它」次要区即可。规格约定：抖音可选，默认第一期可放在次要区，或暂用占位外链

### 明确不展示（或极低优先级，默认不做）
- WhatsApp、Proton（基本不看）

### 占位约定
第一期用假 ID / 假链接占位，结构按上面分组留好（主联系一组 + 次要一组），便于以后只改 content/ 或配置文件。

## 功能
- Markdown：content/ 下 .md 构建时渲染
- 增删项目/文章/服务占位：只改内容文件后重新部署
- 背景图：静态资源 + CSS
- 联系方式：从内容/配置读取分组链接与占位，不写死在布局里（便于迁移）
- 服务卡片：从 content/ 读取标题、简介、CTA 文案与目标（指向 Connect）

## 明确不做
- 留言/评论、注册登录、在线编辑后台
- 数据库与复杂后端
- 第一期不做动态后台；不做 WhatsApp / Proton 展示
- 第一期不做支付、订单表单后端、库存或结账流程
- 不做 VPN / 机场 / 翻墙 / 代理节点分享等业务内容或相关文案
- 不做「后续待办 / roadmap」独立小页（个人站非产品站；规划自用笔记即可）
- 不做多语种 / i18n（第一期仅中文）

## 验收标准
1. 本地可预览；可部署到 Cloudflare Pages
2. 手机/桌面布局清晰，锚点导航可用
3. 占位内容齐全，能完整走通浏览路径（含服务占位 → Connect）
4. 二次元风格可辨且文字可读
5. 说明书：如何把占位换成真实信息（只改哪些文件，含服务卡片）
6. Connect 区块按「主联系 / 次要」两组实现；主联系含 GitHub；无 WhatsApp、无 Proton
7. README 说明「以后若要动态化」的演进注意事项（内容与布局分离），但第一期不做动态
8. 首页「服务」区块或 `/services` 页存在 2～3 张中性占位卡片；CTA 指向联系；无支付/下单后端
9. 文案与内容中无 VPN / 机场 / 翻墙 / 代理节点分享等业务表述

### 轻量加分（非新大功能，有则更好）
- 合理的页面 `title` / `description`（便于分享与书签）
- 简单 favicon
- 移动端可用（布局清晰即可，与上列验收第 2 条一致）

## 占位内容约定（须中文）
- 昵称：昵称占位
- 一句话：用一句话介绍自己（占位）
- 关于我：2～3 段占位 Markdown
- 项目：3 条（标题 + 简介 + 标签）
- 文章：2 篇短示例
- 服务：2～3 条中性占位（例：服务项目占位 A/B/C + 短简介 +「了解更多 / 联系我」CTA → Connect）
- 社交（按分组）：
  - 主联系：QQ / 微信 / X / Telegram / Gmail / GitHub — 假 ID 或假链接
  - 次要：小黑盒 / Steam（可选抖音占位外链）
  - 不放：WhatsApp、Proton
