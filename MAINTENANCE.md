# PersonalWeb 站点日常维护与发布 SOP 手册

本文档为站长（Sutady）日常**撰写博客文章、插入图片、修改个人信息、增删卡片以及 Git 提交流程**的操作指引与避坑防呆手册。

---

## 目录
1. [日常高频更新场景标准流程 (SOP)](#一日常高频更新场景标准流程-sop)
   * [1.1 撰写 / 发布新文章](#11-撰写--发布新文章)
   * [1.2 文章配图规范（彻底杜绝 404 避坑清单）](#12-文章配图规范彻底杜绝-404-避坑清单)
   * [1.3 增删改服务卡片](#13-增删改服务卡片)
   * [1.4 增删改个人历程（时间线 Timeline）卡片](#14-增删改个人历程时间线-timeline-卡片)
   * [1.5 修改个人资料、关于我与社交联系方式](#15-修改个人资料关于我与社交联系方式)
   * [1.6 更换网站头像、插画与全局纹理](#16-更换网站头像插画与全局纹理)
2. [每次发布前的“三步安全检查清单”](#二次发布前的三步安全检查清单)
3. [Git 提交与自动部署命令速查表](#三git-提交与自动部署命令速查表)
4. [常见突发问题应急急救指南](#四常见突发问题应急急救指南)

---

## 一、日常高频更新场景标准流程 (SOP)

### 1.1 撰写 / 发布新文章

* **文章存放路径**：`content/articles/文章英文简称.md`
  > 💡 **命名建议**：文件名会直接变成网页的网址（URL），请统一使用**全小写英文字母 + 下划线或连字符**，例如 `vscode_for_c.md` 或 `docker_beginner_guide.md`，不要包含中文或空格。

* **文章 Frontmatter 必填字段模板**：
  每个 Markdown 文件开头必须包含两行 `---` 之间的 YAML 元数据：

```markdown
---
title: 文章的完整主标题（支持中英文、emoji）
date: 2026-09-07
description: 一句简短的摘要（会显示在文章列表卡片上，以及作为 SEO 描述）
category: 教程
---

## 第一节小标题

这里开始写你的正文内容...
```

* **分类（`category`）规范**：
  * 系统会自动根据所有文章的 `category` 字段生成侧边栏的马卡龙筛选按钮和首页分类胶囊；
  * 推荐分类命名（统一分类名称，避免出现“教程”和“新手教程”导致分类碎片化）：
    * `教程`：安装配置、开发环境、代码实战等实操教学
    * `科普`：计算机网络、软硬件概念、原理讲解
    * `日常`：个人生活、碎碎念、感悟心得
    * `随笔`：技术杂谈、工具推荐、折腾记录

* **排版进阶建议**：
  * **标题层级**：正文从 `## 二级标题` 开始写（一级标题 `#` 会由系统模板自动渲染为页面大标题）；
  * **引言与引用**：使用 `> 引用文本` 渲染优雅的粉灰色引言卡片；
  * **代码高亮**：使用三反引号并标注语言，如 ```` ```python ````、```` ```bash ````、```` ```cpp ````，系统已集成 Shiki 语法高亮。

---

### 1.2 文章配图规范（彻底杜绝 404 避坑清单）

在 Linux 开发环境和生产服务器上，文件路径**严格区分大小写**，请务必遵守以下三大黄金法则：

#### 黄金法则 1：全小写命名法（最重要）
* 图片文件夹名与图片文件名，**一律使用全小写字母 + 下划线或中横线**！
* ❌ 严禁出现大写：`public/images/vscode_for_c/VsCode.png` 或 `VSCode.png`
* ✅ 统一规范：`public/images/vscode_for_c/vscode.png`
* **好处**：无论在任何操作系统、任何编辑器中书写，绝不会因为手指误按 Shift 键导致文件名大小写不一致而 404！

#### 黄金法则 2：使用以 `/` 开头的 Web 根路径
* 图片存放目录：`public/images/你的文章文件夹/你的图片.png`
* Markdown 中的正确写法：
  ```markdown
  ![图片描述](/images/vscode_for_c/vscode.png)
  ```
* ❌ 严禁写相对路径：`./vscode.png` 或 `../images/...`（编译后在浏览器中解析 URL 会错位产生 404）；
* ❌ 严禁写带有 `public` 的路径：`/public/images/...`（打包时 `public` 自身映射为根目录 `/`，路径中不含 `public`）。

#### 黄金法则 3：图片体积与格式建议
* **推荐格式**：首选 `.png` 或 `.jpg`，追求更小体积可选用 `.webp`；
* **单张体积控制**：建议单张图片体积控制在 **500KB 以内**（截图过大时，可先在 [TinyPNG](https://tinypng.com/) 免费压缩一下再放进来），保障手机端秒开加载。

---

### 1.3 增删改服务卡片

* **服务文件目录**：`content/services/`
* **新增服务**：新建 `xxx.md`；**删除服务**：直接删除对应的 `.md` 文件；**修改服务**：直接编辑对应文件。
* **Frontmatter 字段模板**：
```markdown
---
title: 服务项目名称（如：静态网页部署）
summary: 一句简明扼要的服务说明（如：这边可以帮忙部署静态网页哦=w=）
ctaLabel: 了解更多 / 联系我
ctaHref: /#connect
order: 1
---

这里写展开后的详细业务介绍和说明文字。
支持写多段文字、列表或温馨提示。
```
* **说明**：
  * `order`：排序序号，数字越小排在越前面（1, 2, 3...）；
  * `ctaHref`：引导按钮链接，默认指向联系板块 `/#connect`。

---

### 1.4 增删改个人历程（时间线 Timeline）卡片

* **历程文件目录**：`content/timeline/`（如 `01-personal-web.md`）
* **Frontmatter 字段完整模板**：
```markdown
---
date: "2026.09"               # 时间显示标签（如 "2026.09" 或 "Future"）
title: 里程碑标题               # 卡片主标题
tag: 独立开发                  # 右上角粉色药丸标签（如：独立开发 / 科普分享 / 新手起步 / 持续进阶）
isHighlight: true             # 是否为最新高亮节点（为 true 时节点带粉色脉冲呼吸光环）
isFuture: false               # 是否为未来展望节点（为 true 时节点呈慢速旋转蓝色小星星 ★）
order: 1                      # 排序序号，数字越小排在越前面（0 通常留给 Future 卡片）
links:                        # 底部跳转按钮数组（可选，若无跳转可填 links: []）
  - label: 访问 GitHub ↗
    href: https://github.com/TianlinglingOvO/PersonalWeb
    isExternal: true          # 站外链接填 true（在新窗口打开），站内链接填 false
---

卡片正文说明文案，1~2 句话简述这一阶段的经历、感悟与成果。
```
* **编写技巧与规范**：
  * **节点视觉效果**：普通现实节点会自动渲染为温润的粉色圆圈；带有 `isFuture: true` 的节点会自动渲染为梦幻蓝色自转星星（★）；无需配置任何图标，极简省心；
  * **排列顺序**：卡片在桌面端支持滚轮横向左右平滑滑动，`order` 越小排在越左侧；
  * **最新节点**：建议只给最新的一个现实节点打上 `isHighlight: true`，使其拥有脉冲呼吸光环。

---

### 1.5 修改个人资料、关于我与社交联系方式

* **基础网站信息与站名**：`content/site.json`
  * 可修改：站长昵称 `name`、网站大标题 `title`、个性签名 `bio`、页面默认介绍 `description` 等。
* **关于我详细文案**：`content/about.md`
  * 直接使用 Markdown 格式修改关于你的故事、经历、兴趣与爱好。
* **社交与联系方式**：`content/social.json`
  * 包含了 QQ、微信、X、Telegram、Gmail、GitHub 等联系方式；
  * `"action": "link"`：点击在新窗口打开 `href` 网址；
  * `"action": "copy"`：点击自动复制 `value` 内容并弹出“已复制”轻提示（Toast）。

---

### 1.6 更换网站头像、插画与全局纹理

站点使用的全局主视觉素材均存放在 `public/images/` 目录下：

| 图片文件 | 对应显示位置 | 建议尺寸 / 规格 |
| :--- | :--- | :--- |
| `avatar_square.jpg` | 首页 Hero 区域正方形头像 | 建议 1:1 正方形，400×400 或以上高清 Retina 规格 |
| `brand-avatar.jpg` | 顶部导航栏左侧圆形微缩头像 | 建议 1:1 正方形，128×128 像素 |
| `welcome.jpg` | 首页右侧手绘风欢迎插画 | 建议竖版插画，宽高比约 3:4 |
| `bedroom.jpg` | 关于我板块插画 | 建议竖版插画 |
| `rain.jpg` | 联系方式板块插画 | 建议竖版插画 |
| `cake.jpg` | 404 缺省页插画 | 建议插画或趣味方图 |
| `bg-pattern-pastel.svg` | 全局背景粉嫩二次元平铺浮动纹理 | 矢量 SVG 图形 |

> 💡 **防浏览器强缓存小技巧**：
> 如果你替换了同名图片（例如换了新的 `avatar_square.jpg`），有时候浏览器会因为强缓存显示旧图片。解决办法是：在引用该图片的代码里（如 `src/components/Header.astro`），在图片路径后把版本号加一，例如改为 `avatar_square.jpg?v=9`。

---

## 二、每次发布前的“三步安全检查清单”

为了确保每次推送到 GitHub 和线上环境 100% 稳定，请在推送前在本地终端完成以下三步检查：

```
[本地更新] ──> 【Step 1: 启动预览】 ──> 【Step 2: 检查 0 报错】 ──> 【Step 3: 静态打包】 ──> [安全推送]
```

* **Step 1：启动本地开发预览**
  ```bash
  npm run dev
  ```
  在浏览器中打开终端显示的本地地址（通常为 `http://localhost:4321`），检查刚才修改的文章、卡片或文字排版是否美观。

* **Step 2：检查浏览器 Console 是否有 404 红色警告**
  按 `F12` 打开浏览器开发者工具，切换到 **Console（控制台）** 标签页。
  * 确认没有红色的 `404 (Not Found)` 图片报错；
  * 若有 404，立刻根据终端日志核对文件名是否拼写错误或大小写不一致。

* **Step 3：执行正式静态打包预检（必须完全通过）**
  按 `Ctrl + C` 退出预览，在终端运行：
  ```bash
  npm run build
  ```
  * 必须看到终端打印出 `Complete!`，页面生成数量全部成功，**0 错误、0 警告**；
  * 如果 `npm run build` 报错，绝对不要提交 Git，先根据提示修复错误。

---

## 三、Git 提交与自动部署命令速查表

在本地三步检查全部通过后，只需按顺序执行以下 4 条标准 Git 命令即可完成上线：

```bash
# 1. 查看当前改动了哪些文件（确认是否包含你想提交的内容）
git status

# 2. 将所有改动（包括新增加的文件、修改的文件、删除的旧文件）一次性加入暂存区
git add -A

# 3. 提交改动并附带清晰的说明（可参考下方语义化说明）
git commit -m "feat(article): 发布新文章：VSCode安装配置C语言环境"

# 4. 推送到 GitHub 远程仓库的主分支（main）
git push origin main
```

### Commit 提交信息常用前缀推荐：
* `feat:` 新增功能或新文章（如 `feat: 新增文章 what_is_ip`）
* `fix:` 修复 Bug（如 `fix: 修复手机端菜单在下滑时的镂空问题`）
* `docs:` 文档更新（如 `docs: 更新日常维护手册`）
* `style:` 纯样式细节微调（如 `style: 调整分类侧栏阴影与间距`）
* `chore:` 日常小改动、配置文件调整（如 `chore: 升级依赖`）

> 🚀 **推送后的自动化流水线**：
> 推送完成后，GitHub / Cloudflare Pages 会自动监听 `main` 分支的提交，一般在 1~2 分钟内自动完成云端构建部署，线上网址即刻更新生效！

---

## 四、常见突发问题应急急救指南

### 故障 1：新发布的文章图片裂开，控制台报 404
* **排查第 1 步**：在终端运行 `ls public/images/你的文件夹/`，看实际文件名是什么。
* **排查第 2 步**：对比 Markdown 里的路径。注意 Linux 下 `VsCode.png`、`VSCode.png`、`vscode.png` 是三个完全不同的文件！
* **快速解决**：在终端将文件名重命名为全小写，并在 Markdown 中同步改为全小写。

### 故障 2：我不小心在本地删错了文件，怎么找回？
Git 拥有完整的历史记忆，只要还没被 `git push`，随时可以一键复原：
```bash
# 查看被误删的文件状态
git status

# 一键复原某个被删除的文件
git restore content/articles/被删的文件.md

# 或者复原整个工作区的所有未暂存修改
git restore .
```

### 故障 3：修改了图片或者文案，但在手机上打开还是旧的内容？
* **原因**：手机端移动浏览器（Safari / Chrome / 微信内置浏览器）通常有极其强烈的静态资源本地缓存。
* **解决办法**：
  1. 在手机浏览器设置中清除“浏览历史与网站数据”；
  2. 或者开启“无痕浏览 / 隐私标签页”访问站点；
  3. 对于图片素材，在引用路径末尾加版本号，例如 `/images/avatar.jpg?v=2`。

### 故障 4：本地构建成功，但 Cloudflare Pages 线上部署失败？
* 登录 [Cloudflare Dashboard](https://dash.cloudflare.com/) → **Workers & Pages** → 找到本项目；
* 点击失败的部署批次查看 **Build Log（构建日志）**，查看具体报错在哪一个文件。通常是因为某个 Markdown 的 YAML 缺少闭合的 `---` 或者字段拼写错误。
